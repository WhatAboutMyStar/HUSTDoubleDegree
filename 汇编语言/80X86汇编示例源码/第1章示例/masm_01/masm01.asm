.386

DATA SEGMENT USE16
  SUM DB ?
DATA ENDS

STACK SEGMENT USE16
  DB 200 DUP(0)
STACK ENDS

CODE SEGMENT USE16
  ASSUME CS:CODE,DS:DATA,SS:STACK
START:
  MOV AX, DATA
  MOV DS, AX
  
  MOV AL, 3
  ADD AL, 5
  
  MOV SUM, AL
  
  MOV DL, SUM
  ADD DL, 30H
  MOV AH, 2
  INT 21H
  
  MOV AH, 4CH
  INT 21H
CODE ENDS
  END START
  